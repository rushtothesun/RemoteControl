<UserControl x:Class="RemoteControl.RemoteControlGui"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RemoteControl"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="500">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="18" Height="18">
                                <Path x:Name="checkMark" Data="M 0 6 L 4 10 L 10 0" StrokeThickness="2" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="checkMark" Property="Stroke" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="Root" DataContext="{x:Static local:RemoteControlSettings.Instance}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Settings -->
        <GroupBox Grid.Row="0" Header="Settings" Margin="5">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="3">
                    <Label Content="Listen Port:" Width="120" />
                    <TextBox Text="{Binding ListenPort, Mode=TwoWay}" Width="80" />
                </StackPanel>
                <CheckBox Content="Enable Commander Mode (Main PC only)"
                          IsChecked="{Binding IsCommanderMode, Mode=TwoWay}"
                          Margin="5" />
            </StackPanel>
        </GroupBox>

        <!-- Remote Bots (commander only) -->
        <GroupBox Grid.Row="1" Header="Remote Bots (IP:Port)" Margin="5"
                  Visibility="{Binding IsCommanderMode, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="3">
                    <TextBox x:Name="NewBotAddress" Width="140" />
                    <TextBox x:Name="NewBotLabel" Width="80" Margin="5,0,0,0" />
                    <Button Content="Add" Margin="5,0" Padding="10,2" Click="AddBot_Click" />
                    <Button Content="Remove" Margin="2,0" Padding="10,2" Click="RemoveBot_Click" />
                </StackPanel>
                <TextBlock Text="Format: IP:Port in first box, display name in second box" 
                           FontSize="10" Foreground="Gray" Margin="5,2" />
                <ListBox x:Name="BotListBox"
                         ItemsSource="{Binding RemoteBots}"
                         Height="60"
                         Margin="3" />
            </StackPanel>
        </GroupBox>

        <!-- Sync control (commander only) -->
        <StackPanel Grid.Row="2" Margin="5,2"
                    Visibility="{Binding IsCommanderMode, Converter={StaticResource BoolToVis}}">
            <CheckBox x:Name="SyncAllCheckbox" Content="Sync All — commands apply to all bots at once"
                      IsChecked="True" Margin="5" FontWeight="Bold" 
                      Checked="SyncAllCheckbox_Changed" Unchecked="SyncAllCheckbox_Changed" />
            <TextBlock x:Name="StatusText" Text="Ready" Margin="5,0" FontSize="11" />
        </StackPanel>

        <!-- Dynamic bot command panels (commander only) -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="5,0,5,5"
                      Visibility="{Binding IsCommanderMode, Converter={StaticResource BoolToVis}}">
            <WrapPanel x:Name="BotPanelsContainer" Orientation="Horizontal" />
        </ScrollViewer>
    </Grid>
</UserControl>
